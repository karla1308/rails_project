<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "Rails Project" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= stylesheet_link_tag :main %>
  </head>

  <body style="background-color:#fcd6db;">

    <nav class="navbar navbar-expand-lg bg-body-tertiary">
      <div class="container-fluid" style="background-color:#fcd6db;">
        <a class="navbar-brand" href="<%= home_index_url %>"> <%= image_tag("kittydance.gif",:class=>"d-inline-block align-top imgset")%></a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav">
            <li class="nav-item" style="padding:2px;">
              <a class="nav-link active rounded nset" aria-current="page" href="<%= home_index_url%>"><i class="fas fa-home"></i> Home</a>
            </li>
            <li class="nav-item" style="padding:2px;">
              <a class="nav-link rounded nset" href="<%= home_school_url %>"><i class="fas fa-school"></i> School</a>
            </li>
            <li class="nav-item" style="padding:2px;">
              <a class="nav-link rounded nset" href="<%= home_work_url %>"><i class="fas fa-dollar-sign"></i> Work & Skills</a>
            </li>
            <li class="nav-item" style="padding:2px;">
              <a class="nav-link rounded nset" href="<%= home_hobbies_url %>"><i class="far fa-heart"></i> Hobbies</a>
            </li>
          </ul>
        </div>
      </div>
    </nav>
    <div class="container-fluid">
      <%= yield %>
    </div>

  </body>
</html>
